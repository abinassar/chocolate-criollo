import { CutLetterPipe } from './cut-letter.pipe';

describe('CutLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new CutLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
